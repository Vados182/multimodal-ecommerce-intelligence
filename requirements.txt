# Web Framework & Cloud Deployment

fastapi>=0.100.0        # framework do budowy nowoczesnych API w Pythonie, używany w projekcie do tworzenia interfejsów RESTful oraz obsługi żądań HTTP.
uvicorn>=0.22.0         # serwer ASGI do uruchamiania aplikacji FastAPI, zapewniający wysoką wydajność i obsługę asynchronicznych żądań.

# Big Data & SQL
pyspark>=3.5.0                  # framework do przetwarzania dużych zbiorów danych w rozproszonym środowisku, używany w projekcie do analizy danych i transformacji danych w formacie Parquet.
google-cloud-bigquery>=3.15.0   # biblioteka do interakcji z usługą Google BigQuery, umożliwiająca wykonywanie zapytań SQL oraz zarządzanie danymi w chmurze Google Cloud.
pandas>=2.0.0                   # biblioteka do analizy danych w Pythonie, używana w projekcie do manipulacji danymi, przetwarzania danych w formacie DataFrame oraz integracji z innymi narzędziami analitycznymi.
numpy>=1.24.0                   # biblioteka do obliczeń numerycznych w Pythonie, używana w projekcie do przetwarzania danych numerycznych.

# Computer Vision & Deep Learning
torch>=2.0.0                    # biblioteka do uczenia maszynowego i głębokiego uczenia, używana w projekcie do tworzenia i trenowania modeli sieci neuronowych oraz przetwarzania danych w formacie tensorów.
torchvision>=0.15.0             # biblioteka do przetwarzania obrazów i wideo w Pythonie, używana w projekcie do pracy z danymi wizualnymi oraz integracji z modelami głębokiego uczenia.
opencv-python-headless>=4.8.0   # biblioteka do przetwarzania obrazów i wideo w Pythonie, używana w projekcie do analizy danych wizualnych oraz integracji z modelami głębokiego uczenia, bez zależności od interfejsu graficznego (headless).

# Machine Learning & Causal Inference
scikit-learn>=1.3.0 # biblioteka do uczenia maszynowego w Pythonie, używana w projekcie do tworzenia i oceny modeli uczenia maszynowego.
xgboost>=2.0.0      # biblioteka do uczenia maszynowego z wykorzystaniem drzew decyzyjnych, używana w projekcie do tworzenia modeli predykcyjnych.
dowhy>=0.10         # biblioteka do wnioskowania przyczynowego, używana w projekcie do analizy relacji przyczynowych między zmiennymi.

# LLM & Agentic AI
langchain>=0.1.0        # biblioteka do tworzenia aplikacji opartych na modelach językowych, używana w projekcie do integracji z modelami LLM oraz tworzenia agentów AI.  
langchain-openai>=0.0.5 # biblioteka do integracji LangChain z API OpenAI, umożliwiająca korzystanie z modeli językowych OpenAI w aplikacjach opartych na LangChain.
pydantic>=2.0.0         # biblioteka do walidacji danych i tworzenia modeli danych w Pythonie, używana w projekcie do definiowania struktur danych oraz walidacji wejściowych danych w aplikacjach opartych na FastAPI i LangChain.

# Helpers & Environment
python-dotenv>=1.0.0     # biblioteka do zarządzania zmiennymi środowiskowymi w Pythonie, używana w projekcie do ładowania konfiguracji z plików .env oraz zarządzania ustawieniami aplikacji w różnych środowiskach.
pytest>=7.4.0            # framework do testowania w Pythonie, używany w projekcie do tworzenia i uruchamiania testów jednostkowych oraz integracyjnych.
pyarrow>=12.0.0          # wymagany przez pandas oraz pyspark do wydajnego odczytu i zapisu plików w formacie Parquet (używamy go w spark_etl.py)
pillow>=10.0.0           # wymagany przez opencv-python-headless do obsługi obrazów w formacie JPEG/PNG (używamy go w image_processing.py)
requests>=2.31.0         # podstawowa biblioteka do wykonywania zapytań HTTP, używana w wielu miejscach w projekcie, np. do pobierania danych z API lub wysyłania wyników do zewnętrznych serwisów.
python-multipart>=0.0.6  # biblioteka do obsługi przesyłania plików w formacie multipart/form-data, używana w projekcie do obsługi przesyłania plików w aplikacjach FastAPI.